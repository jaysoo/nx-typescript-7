export interface BaseP020F63 {
  id: string;
  createdAt: number;
  updatedAt: number;
  revision: number;
  meta: Readonly<Record<string, string | number | boolean | null>>;
}

export interface RecP020F63x0 extends BaseP020F63 {
  kind: 'k-P020F63-0';
  label: string;
  weight: number;
  rank: 0;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x1 extends BaseP020F63 {
  kind: 'k-P020F63-1';
  label: string;
  weight: number;
  rank: 1;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x2 extends BaseP020F63 {
  kind: 'k-P020F63-2';
  label: string;
  weight: number;
  rank: 2;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x3 extends BaseP020F63 {
  kind: 'k-P020F63-3';
  label: string;
  weight: number;
  rank: 3;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x4 extends BaseP020F63 {
  kind: 'k-P020F63-4';
  label: string;
  weight: number;
  rank: 4;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x5 extends BaseP020F63 {
  kind: 'k-P020F63-5';
  label: string;
  weight: number;
  rank: 5;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x6 extends BaseP020F63 {
  kind: 'k-P020F63-6';
  label: string;
  weight: number;
  rank: 6;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x7 extends BaseP020F63 {
  kind: 'k-P020F63-7';
  label: string;
  weight: number;
  rank: 7;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x8 extends BaseP020F63 {
  kind: 'k-P020F63-8';
  label: string;
  weight: number;
  rank: 8;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x9 extends BaseP020F63 {
  kind: 'k-P020F63-9';
  label: string;
  weight: number;
  rank: 9;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x10 extends BaseP020F63 {
  kind: 'k-P020F63-10';
  label: string;
  weight: number;
  rank: 10;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x11 extends BaseP020F63 {
  kind: 'k-P020F63-11';
  label: string;
  weight: number;
  rank: 11;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x12 extends BaseP020F63 {
  kind: 'k-P020F63-12';
  label: string;
  weight: number;
  rank: 12;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x13 extends BaseP020F63 {
  kind: 'k-P020F63-13';
  label: string;
  weight: number;
  rank: 13;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x14 extends BaseP020F63 {
  kind: 'k-P020F63-14';
  label: string;
  weight: number;
  rank: 14;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x15 extends BaseP020F63 {
  kind: 'k-P020F63-15';
  label: string;
  weight: number;
  rank: 15;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x16 extends BaseP020F63 {
  kind: 'k-P020F63-16';
  label: string;
  weight: number;
  rank: 16;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x17 extends BaseP020F63 {
  kind: 'k-P020F63-17';
  label: string;
  weight: number;
  rank: 17;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x18 extends BaseP020F63 {
  kind: 'k-P020F63-18';
  label: string;
  weight: number;
  rank: 18;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x19 extends BaseP020F63 {
  kind: 'k-P020F63-19';
  label: string;
  weight: number;
  rank: 19;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x20 extends BaseP020F63 {
  kind: 'k-P020F63-20';
  label: string;
  weight: number;
  rank: 20;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x21 extends BaseP020F63 {
  kind: 'k-P020F63-21';
  label: string;
  weight: number;
  rank: 21;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x22 extends BaseP020F63 {
  kind: 'k-P020F63-22';
  label: string;
  weight: number;
  rank: 22;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x23 extends BaseP020F63 {
  kind: 'k-P020F63-23';
  label: string;
  weight: number;
  rank: 23;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x24 extends BaseP020F63 {
  kind: 'k-P020F63-24';
  label: string;
  weight: number;
  rank: 24;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x25 extends BaseP020F63 {
  kind: 'k-P020F63-25';
  label: string;
  weight: number;
  rank: 25;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x26 extends BaseP020F63 {
  kind: 'k-P020F63-26';
  label: string;
  weight: number;
  rank: 26;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x27 extends BaseP020F63 {
  kind: 'k-P020F63-27';
  label: string;
  weight: number;
  rank: 27;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x28 extends BaseP020F63 {
  kind: 'k-P020F63-28';
  label: string;
  weight: number;
  rank: 28;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x29 extends BaseP020F63 {
  kind: 'k-P020F63-29';
  label: string;
  weight: number;
  rank: 29;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x30 extends BaseP020F63 {
  kind: 'k-P020F63-30';
  label: string;
  weight: number;
  rank: 30;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x31 extends BaseP020F63 {
  kind: 'k-P020F63-31';
  label: string;
  weight: number;
  rank: 31;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x32 extends BaseP020F63 {
  kind: 'k-P020F63-32';
  label: string;
  weight: number;
  rank: 32;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x33 extends BaseP020F63 {
  kind: 'k-P020F63-33';
  label: string;
  weight: number;
  rank: 33;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x34 extends BaseP020F63 {
  kind: 'k-P020F63-34';
  label: string;
  weight: number;
  rank: 34;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x35 extends BaseP020F63 {
  kind: 'k-P020F63-35';
  label: string;
  weight: number;
  rank: 35;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x36 extends BaseP020F63 {
  kind: 'k-P020F63-36';
  label: string;
  weight: number;
  rank: 36;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x37 extends BaseP020F63 {
  kind: 'k-P020F63-37';
  label: string;
  weight: number;
  rank: 37;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x38 extends BaseP020F63 {
  kind: 'k-P020F63-38';
  label: string;
  weight: number;
  rank: 38;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x39 extends BaseP020F63 {
  kind: 'k-P020F63-39';
  label: string;
  weight: number;
  rank: 39;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x40 extends BaseP020F63 {
  kind: 'k-P020F63-40';
  label: string;
  weight: number;
  rank: 40;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x41 extends BaseP020F63 {
  kind: 'k-P020F63-41';
  label: string;
  weight: number;
  rank: 41;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x42 extends BaseP020F63 {
  kind: 'k-P020F63-42';
  label: string;
  weight: number;
  rank: 42;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x43 extends BaseP020F63 {
  kind: 'k-P020F63-43';
  label: string;
  weight: number;
  rank: 43;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x44 extends BaseP020F63 {
  kind: 'k-P020F63-44';
  label: string;
  weight: number;
  rank: 44;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x45 extends BaseP020F63 {
  kind: 'k-P020F63-45';
  label: string;
  weight: number;
  rank: 45;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x46 extends BaseP020F63 {
  kind: 'k-P020F63-46';
  label: string;
  weight: number;
  rank: 46;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F63x47 extends BaseP020F63 {
  kind: 'k-P020F63-47';
  label: string;
  weight: number;
  rank: 47;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export type AnyP020F63 =
  | RecP020F63x0
  | RecP020F63x1
  | RecP020F63x2
  | RecP020F63x3
  | RecP020F63x4
  | RecP020F63x5
  | RecP020F63x6
  | RecP020F63x7
  | RecP020F63x8
  | RecP020F63x9
  | RecP020F63x10
  | RecP020F63x11
  | RecP020F63x12
  | RecP020F63x13
  | RecP020F63x14
  | RecP020F63x15
  | RecP020F63x16
  | RecP020F63x17
  | RecP020F63x18
  | RecP020F63x19
  | RecP020F63x20
  | RecP020F63x21
  | RecP020F63x22
  | RecP020F63x23
  | RecP020F63x24
  | RecP020F63x25
  | RecP020F63x26
  | RecP020F63x27
  | RecP020F63x28
  | RecP020F63x29
  | RecP020F63x30
  | RecP020F63x31
  | RecP020F63x32
  | RecP020F63x33
  | RecP020F63x34
  | RecP020F63x35
  | RecP020F63x36
  | RecP020F63x37
  | RecP020F63x38
  | RecP020F63x39
  | RecP020F63x40
  | RecP020F63x41
  | RecP020F63x42
  | RecP020F63x43
  | RecP020F63x44
  | RecP020F63x45
  | RecP020F63x46
  | RecP020F63x47;

export type KindP020F63 = AnyP020F63['kind'];

export type ByKindP020F63 = { [R in AnyP020F63 as R['kind']]: R };

export type PatchP020F63<T> = { [K in keyof T]?: T[K] extends object ? PatchP020F63<T[K]> : T[K] };

export type SummaryP020F63 = {
  [K in KindP020F63]: { count: number; weight: number; ranks: number[] };
};

const kindsP020F63 = ['k-P020F63-0', 'k-P020F63-1', 'k-P020F63-2', 'k-P020F63-3', 'k-P020F63-4', 'k-P020F63-5', 'k-P020F63-6', 'k-P020F63-7', 'k-P020F63-8', 'k-P020F63-9', 'k-P020F63-10', 'k-P020F63-11', 'k-P020F63-12', 'k-P020F63-13', 'k-P020F63-14', 'k-P020F63-15', 'k-P020F63-16', 'k-P020F63-17', 'k-P020F63-18', 'k-P020F63-19', 'k-P020F63-20', 'k-P020F63-21', 'k-P020F63-22', 'k-P020F63-23', 'k-P020F63-24', 'k-P020F63-25', 'k-P020F63-26', 'k-P020F63-27', 'k-P020F63-28', 'k-P020F63-29', 'k-P020F63-30', 'k-P020F63-31', 'k-P020F63-32', 'k-P020F63-33', 'k-P020F63-34', 'k-P020F63-35', 'k-P020F63-36', 'k-P020F63-37', 'k-P020F63-38', 'k-P020F63-39', 'k-P020F63-40', 'k-P020F63-41', 'k-P020F63-42', 'k-P020F63-43', 'k-P020F63-44', 'k-P020F63-45', 'k-P020F63-46', 'k-P020F63-47'] as const;

let seqP020F63 = 0;

export function createP020F63<K extends KindP020F63>(kind: K, label: string, weight: number): ByKindP020F63[K] {
  seqP020F63 += 1;
  const rank = kindsP020F63.indexOf(kind);
  const rec = {
    id: `${kind}-${seqP020F63}`,
    createdAt: seqP020F63,
    updatedAt: seqP020F63,
    revision: 1,
    meta: { seq: seqP020F63, source: label },
    kind,
    label,
    weight,
    rank,
    tags: [label, kind],
    payload: { values: [weight, rank], flags: { fresh: true }, note: label },
  };
  return rec as unknown as ByKindP020F63[K];
}

export function batchP020F63(count: number): AnyP020F63[] {
  const out: AnyP020F63[] = [];
  for (let idx = 0; idx < count; idx++) {
    out.push(createP020F63(kindsP020F63[idx % kindsP020F63.length], `item-${idx}`, idx * 0.5));
  }
  return out;
}

export function pickP020F63<T extends object, K extends keyof T>(obj: T, keys: readonly K[]): Pick<T, K> {
  const out = {} as Pick<T, K>;
  for (const key of keys) {
    out[key] = obj[key];
  }
  return out;
}

export function groupByP020F63(records: readonly AnyP020F63[]): Partial<{ [K in KindP020F63]: ByKindP020F63[K][] }> {
  const out: Partial<{ [K in KindP020F63]: ByKindP020F63[K][] }> = {};
  for (const rec of records) {
    const bucket = (out[rec.kind] ??= []);
    (bucket as AnyP020F63[]).push(rec);
  }
  return out;
}

export function summarizeP020F63(records: readonly AnyP020F63[]): Partial<SummaryP020F63> {
  const grouped = groupByP020F63(records);
  const out: Partial<SummaryP020F63> = {};
  for (const kind of kindsP020F63) {
    const bucket = grouped[kind];
    if (!bucket) continue;
    out[kind] = {
      count: bucket.length,
      weight: bucket.reduce((sum, r) => sum + r.weight, 0),
      ranks: bucket.map((r) => r.rank as number),
    };
  }
  return out;
}

export function mergeP020F63<T extends BaseP020F63>(target: T, patch: PatchP020F63<T>): T {
  return { ...target, ...patch, revision: target.revision + 1 };
}

export function totalWeightP020F63(records: readonly AnyP020F63[]): number {
  const summary = summarizeP020F63(records);
  let total = 0;
  for (const kind of kindsP020F63) {
    total += summary[kind]?.weight ?? 0;
  }
  return total;
}
