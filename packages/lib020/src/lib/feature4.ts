export interface BaseP020F4 {
  id: string;
  createdAt: number;
  updatedAt: number;
  revision: number;
  meta: Readonly<Record<string, string | number | boolean | null>>;
}

export interface RecP020F4x0 extends BaseP020F4 {
  kind: 'k-P020F4-0';
  label: string;
  weight: number;
  rank: 0;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x1 extends BaseP020F4 {
  kind: 'k-P020F4-1';
  label: string;
  weight: number;
  rank: 1;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x2 extends BaseP020F4 {
  kind: 'k-P020F4-2';
  label: string;
  weight: number;
  rank: 2;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x3 extends BaseP020F4 {
  kind: 'k-P020F4-3';
  label: string;
  weight: number;
  rank: 3;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x4 extends BaseP020F4 {
  kind: 'k-P020F4-4';
  label: string;
  weight: number;
  rank: 4;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x5 extends BaseP020F4 {
  kind: 'k-P020F4-5';
  label: string;
  weight: number;
  rank: 5;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x6 extends BaseP020F4 {
  kind: 'k-P020F4-6';
  label: string;
  weight: number;
  rank: 6;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x7 extends BaseP020F4 {
  kind: 'k-P020F4-7';
  label: string;
  weight: number;
  rank: 7;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x8 extends BaseP020F4 {
  kind: 'k-P020F4-8';
  label: string;
  weight: number;
  rank: 8;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x9 extends BaseP020F4 {
  kind: 'k-P020F4-9';
  label: string;
  weight: number;
  rank: 9;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x10 extends BaseP020F4 {
  kind: 'k-P020F4-10';
  label: string;
  weight: number;
  rank: 10;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x11 extends BaseP020F4 {
  kind: 'k-P020F4-11';
  label: string;
  weight: number;
  rank: 11;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x12 extends BaseP020F4 {
  kind: 'k-P020F4-12';
  label: string;
  weight: number;
  rank: 12;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x13 extends BaseP020F4 {
  kind: 'k-P020F4-13';
  label: string;
  weight: number;
  rank: 13;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x14 extends BaseP020F4 {
  kind: 'k-P020F4-14';
  label: string;
  weight: number;
  rank: 14;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x15 extends BaseP020F4 {
  kind: 'k-P020F4-15';
  label: string;
  weight: number;
  rank: 15;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x16 extends BaseP020F4 {
  kind: 'k-P020F4-16';
  label: string;
  weight: number;
  rank: 16;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x17 extends BaseP020F4 {
  kind: 'k-P020F4-17';
  label: string;
  weight: number;
  rank: 17;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x18 extends BaseP020F4 {
  kind: 'k-P020F4-18';
  label: string;
  weight: number;
  rank: 18;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x19 extends BaseP020F4 {
  kind: 'k-P020F4-19';
  label: string;
  weight: number;
  rank: 19;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x20 extends BaseP020F4 {
  kind: 'k-P020F4-20';
  label: string;
  weight: number;
  rank: 20;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x21 extends BaseP020F4 {
  kind: 'k-P020F4-21';
  label: string;
  weight: number;
  rank: 21;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x22 extends BaseP020F4 {
  kind: 'k-P020F4-22';
  label: string;
  weight: number;
  rank: 22;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x23 extends BaseP020F4 {
  kind: 'k-P020F4-23';
  label: string;
  weight: number;
  rank: 23;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x24 extends BaseP020F4 {
  kind: 'k-P020F4-24';
  label: string;
  weight: number;
  rank: 24;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x25 extends BaseP020F4 {
  kind: 'k-P020F4-25';
  label: string;
  weight: number;
  rank: 25;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x26 extends BaseP020F4 {
  kind: 'k-P020F4-26';
  label: string;
  weight: number;
  rank: 26;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x27 extends BaseP020F4 {
  kind: 'k-P020F4-27';
  label: string;
  weight: number;
  rank: 27;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x28 extends BaseP020F4 {
  kind: 'k-P020F4-28';
  label: string;
  weight: number;
  rank: 28;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x29 extends BaseP020F4 {
  kind: 'k-P020F4-29';
  label: string;
  weight: number;
  rank: 29;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x30 extends BaseP020F4 {
  kind: 'k-P020F4-30';
  label: string;
  weight: number;
  rank: 30;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x31 extends BaseP020F4 {
  kind: 'k-P020F4-31';
  label: string;
  weight: number;
  rank: 31;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x32 extends BaseP020F4 {
  kind: 'k-P020F4-32';
  label: string;
  weight: number;
  rank: 32;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x33 extends BaseP020F4 {
  kind: 'k-P020F4-33';
  label: string;
  weight: number;
  rank: 33;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x34 extends BaseP020F4 {
  kind: 'k-P020F4-34';
  label: string;
  weight: number;
  rank: 34;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x35 extends BaseP020F4 {
  kind: 'k-P020F4-35';
  label: string;
  weight: number;
  rank: 35;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x36 extends BaseP020F4 {
  kind: 'k-P020F4-36';
  label: string;
  weight: number;
  rank: 36;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x37 extends BaseP020F4 {
  kind: 'k-P020F4-37';
  label: string;
  weight: number;
  rank: 37;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x38 extends BaseP020F4 {
  kind: 'k-P020F4-38';
  label: string;
  weight: number;
  rank: 38;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x39 extends BaseP020F4 {
  kind: 'k-P020F4-39';
  label: string;
  weight: number;
  rank: 39;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x40 extends BaseP020F4 {
  kind: 'k-P020F4-40';
  label: string;
  weight: number;
  rank: 40;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x41 extends BaseP020F4 {
  kind: 'k-P020F4-41';
  label: string;
  weight: number;
  rank: 41;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x42 extends BaseP020F4 {
  kind: 'k-P020F4-42';
  label: string;
  weight: number;
  rank: 42;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x43 extends BaseP020F4 {
  kind: 'k-P020F4-43';
  label: string;
  weight: number;
  rank: 43;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x44 extends BaseP020F4 {
  kind: 'k-P020F4-44';
  label: string;
  weight: number;
  rank: 44;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x45 extends BaseP020F4 {
  kind: 'k-P020F4-45';
  label: string;
  weight: number;
  rank: 45;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x46 extends BaseP020F4 {
  kind: 'k-P020F4-46';
  label: string;
  weight: number;
  rank: 46;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP020F4x47 extends BaseP020F4 {
  kind: 'k-P020F4-47';
  label: string;
  weight: number;
  rank: 47;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export type AnyP020F4 =
  | RecP020F4x0
  | RecP020F4x1
  | RecP020F4x2
  | RecP020F4x3
  | RecP020F4x4
  | RecP020F4x5
  | RecP020F4x6
  | RecP020F4x7
  | RecP020F4x8
  | RecP020F4x9
  | RecP020F4x10
  | RecP020F4x11
  | RecP020F4x12
  | RecP020F4x13
  | RecP020F4x14
  | RecP020F4x15
  | RecP020F4x16
  | RecP020F4x17
  | RecP020F4x18
  | RecP020F4x19
  | RecP020F4x20
  | RecP020F4x21
  | RecP020F4x22
  | RecP020F4x23
  | RecP020F4x24
  | RecP020F4x25
  | RecP020F4x26
  | RecP020F4x27
  | RecP020F4x28
  | RecP020F4x29
  | RecP020F4x30
  | RecP020F4x31
  | RecP020F4x32
  | RecP020F4x33
  | RecP020F4x34
  | RecP020F4x35
  | RecP020F4x36
  | RecP020F4x37
  | RecP020F4x38
  | RecP020F4x39
  | RecP020F4x40
  | RecP020F4x41
  | RecP020F4x42
  | RecP020F4x43
  | RecP020F4x44
  | RecP020F4x45
  | RecP020F4x46
  | RecP020F4x47;

export type KindP020F4 = AnyP020F4['kind'];

export type ByKindP020F4 = { [R in AnyP020F4 as R['kind']]: R };

export type PatchP020F4<T> = { [K in keyof T]?: T[K] extends object ? PatchP020F4<T[K]> : T[K] };

export type SummaryP020F4 = {
  [K in KindP020F4]: { count: number; weight: number; ranks: number[] };
};

const kindsP020F4 = ['k-P020F4-0', 'k-P020F4-1', 'k-P020F4-2', 'k-P020F4-3', 'k-P020F4-4', 'k-P020F4-5', 'k-P020F4-6', 'k-P020F4-7', 'k-P020F4-8', 'k-P020F4-9', 'k-P020F4-10', 'k-P020F4-11', 'k-P020F4-12', 'k-P020F4-13', 'k-P020F4-14', 'k-P020F4-15', 'k-P020F4-16', 'k-P020F4-17', 'k-P020F4-18', 'k-P020F4-19', 'k-P020F4-20', 'k-P020F4-21', 'k-P020F4-22', 'k-P020F4-23', 'k-P020F4-24', 'k-P020F4-25', 'k-P020F4-26', 'k-P020F4-27', 'k-P020F4-28', 'k-P020F4-29', 'k-P020F4-30', 'k-P020F4-31', 'k-P020F4-32', 'k-P020F4-33', 'k-P020F4-34', 'k-P020F4-35', 'k-P020F4-36', 'k-P020F4-37', 'k-P020F4-38', 'k-P020F4-39', 'k-P020F4-40', 'k-P020F4-41', 'k-P020F4-42', 'k-P020F4-43', 'k-P020F4-44', 'k-P020F4-45', 'k-P020F4-46', 'k-P020F4-47'] as const;

let seqP020F4 = 0;

export function createP020F4<K extends KindP020F4>(kind: K, label: string, weight: number): ByKindP020F4[K] {
  seqP020F4 += 1;
  const rank = kindsP020F4.indexOf(kind);
  const rec = {
    id: `${kind}-${seqP020F4}`,
    createdAt: seqP020F4,
    updatedAt: seqP020F4,
    revision: 1,
    meta: { seq: seqP020F4, source: label },
    kind,
    label,
    weight,
    rank,
    tags: [label, kind],
    payload: { values: [weight, rank], flags: { fresh: true }, note: label },
  };
  return rec as unknown as ByKindP020F4[K];
}

export function batchP020F4(count: number): AnyP020F4[] {
  const out: AnyP020F4[] = [];
  for (let idx = 0; idx < count; idx++) {
    out.push(createP020F4(kindsP020F4[idx % kindsP020F4.length], `item-${idx}`, idx * 0.5));
  }
  return out;
}

export function pickP020F4<T extends object, K extends keyof T>(obj: T, keys: readonly K[]): Pick<T, K> {
  const out = {} as Pick<T, K>;
  for (const key of keys) {
    out[key] = obj[key];
  }
  return out;
}

export function groupByP020F4(records: readonly AnyP020F4[]): Partial<{ [K in KindP020F4]: ByKindP020F4[K][] }> {
  const out: Partial<{ [K in KindP020F4]: ByKindP020F4[K][] }> = {};
  for (const rec of records) {
    const bucket = (out[rec.kind] ??= []);
    (bucket as AnyP020F4[]).push(rec);
  }
  return out;
}

export function summarizeP020F4(records: readonly AnyP020F4[]): Partial<SummaryP020F4> {
  const grouped = groupByP020F4(records);
  const out: Partial<SummaryP020F4> = {};
  for (const kind of kindsP020F4) {
    const bucket = grouped[kind];
    if (!bucket) continue;
    out[kind] = {
      count: bucket.length,
      weight: bucket.reduce((sum, r) => sum + r.weight, 0),
      ranks: bucket.map((r) => r.rank as number),
    };
  }
  return out;
}

export function mergeP020F4<T extends BaseP020F4>(target: T, patch: PatchP020F4<T>): T {
  return { ...target, ...patch, revision: target.revision + 1 };
}

export function totalWeightP020F4(records: readonly AnyP020F4[]): number {
  const summary = summarizeP020F4(records);
  let total = 0;
  for (const kind of kindsP020F4) {
    total += summary[kind]?.weight ?? 0;
  }
  return total;
}
