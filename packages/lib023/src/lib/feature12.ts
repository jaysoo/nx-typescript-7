export interface BaseP023F12 {
  id: string;
  createdAt: number;
  updatedAt: number;
  revision: number;
  meta: Readonly<Record<string, string | number | boolean | null>>;
}

export interface RecP023F12x0 extends BaseP023F12 {
  kind: 'k-P023F12-0';
  label: string;
  weight: number;
  rank: 0;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x1 extends BaseP023F12 {
  kind: 'k-P023F12-1';
  label: string;
  weight: number;
  rank: 1;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x2 extends BaseP023F12 {
  kind: 'k-P023F12-2';
  label: string;
  weight: number;
  rank: 2;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x3 extends BaseP023F12 {
  kind: 'k-P023F12-3';
  label: string;
  weight: number;
  rank: 3;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x4 extends BaseP023F12 {
  kind: 'k-P023F12-4';
  label: string;
  weight: number;
  rank: 4;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x5 extends BaseP023F12 {
  kind: 'k-P023F12-5';
  label: string;
  weight: number;
  rank: 5;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x6 extends BaseP023F12 {
  kind: 'k-P023F12-6';
  label: string;
  weight: number;
  rank: 6;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x7 extends BaseP023F12 {
  kind: 'k-P023F12-7';
  label: string;
  weight: number;
  rank: 7;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x8 extends BaseP023F12 {
  kind: 'k-P023F12-8';
  label: string;
  weight: number;
  rank: 8;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x9 extends BaseP023F12 {
  kind: 'k-P023F12-9';
  label: string;
  weight: number;
  rank: 9;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x10 extends BaseP023F12 {
  kind: 'k-P023F12-10';
  label: string;
  weight: number;
  rank: 10;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x11 extends BaseP023F12 {
  kind: 'k-P023F12-11';
  label: string;
  weight: number;
  rank: 11;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x12 extends BaseP023F12 {
  kind: 'k-P023F12-12';
  label: string;
  weight: number;
  rank: 12;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x13 extends BaseP023F12 {
  kind: 'k-P023F12-13';
  label: string;
  weight: number;
  rank: 13;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x14 extends BaseP023F12 {
  kind: 'k-P023F12-14';
  label: string;
  weight: number;
  rank: 14;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x15 extends BaseP023F12 {
  kind: 'k-P023F12-15';
  label: string;
  weight: number;
  rank: 15;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x16 extends BaseP023F12 {
  kind: 'k-P023F12-16';
  label: string;
  weight: number;
  rank: 16;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x17 extends BaseP023F12 {
  kind: 'k-P023F12-17';
  label: string;
  weight: number;
  rank: 17;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x18 extends BaseP023F12 {
  kind: 'k-P023F12-18';
  label: string;
  weight: number;
  rank: 18;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x19 extends BaseP023F12 {
  kind: 'k-P023F12-19';
  label: string;
  weight: number;
  rank: 19;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x20 extends BaseP023F12 {
  kind: 'k-P023F12-20';
  label: string;
  weight: number;
  rank: 20;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x21 extends BaseP023F12 {
  kind: 'k-P023F12-21';
  label: string;
  weight: number;
  rank: 21;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x22 extends BaseP023F12 {
  kind: 'k-P023F12-22';
  label: string;
  weight: number;
  rank: 22;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x23 extends BaseP023F12 {
  kind: 'k-P023F12-23';
  label: string;
  weight: number;
  rank: 23;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x24 extends BaseP023F12 {
  kind: 'k-P023F12-24';
  label: string;
  weight: number;
  rank: 24;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x25 extends BaseP023F12 {
  kind: 'k-P023F12-25';
  label: string;
  weight: number;
  rank: 25;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x26 extends BaseP023F12 {
  kind: 'k-P023F12-26';
  label: string;
  weight: number;
  rank: 26;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x27 extends BaseP023F12 {
  kind: 'k-P023F12-27';
  label: string;
  weight: number;
  rank: 27;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x28 extends BaseP023F12 {
  kind: 'k-P023F12-28';
  label: string;
  weight: number;
  rank: 28;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x29 extends BaseP023F12 {
  kind: 'k-P023F12-29';
  label: string;
  weight: number;
  rank: 29;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x30 extends BaseP023F12 {
  kind: 'k-P023F12-30';
  label: string;
  weight: number;
  rank: 30;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x31 extends BaseP023F12 {
  kind: 'k-P023F12-31';
  label: string;
  weight: number;
  rank: 31;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x32 extends BaseP023F12 {
  kind: 'k-P023F12-32';
  label: string;
  weight: number;
  rank: 32;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x33 extends BaseP023F12 {
  kind: 'k-P023F12-33';
  label: string;
  weight: number;
  rank: 33;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x34 extends BaseP023F12 {
  kind: 'k-P023F12-34';
  label: string;
  weight: number;
  rank: 34;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x35 extends BaseP023F12 {
  kind: 'k-P023F12-35';
  label: string;
  weight: number;
  rank: 35;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x36 extends BaseP023F12 {
  kind: 'k-P023F12-36';
  label: string;
  weight: number;
  rank: 36;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x37 extends BaseP023F12 {
  kind: 'k-P023F12-37';
  label: string;
  weight: number;
  rank: 37;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x38 extends BaseP023F12 {
  kind: 'k-P023F12-38';
  label: string;
  weight: number;
  rank: 38;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x39 extends BaseP023F12 {
  kind: 'k-P023F12-39';
  label: string;
  weight: number;
  rank: 39;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x40 extends BaseP023F12 {
  kind: 'k-P023F12-40';
  label: string;
  weight: number;
  rank: 40;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x41 extends BaseP023F12 {
  kind: 'k-P023F12-41';
  label: string;
  weight: number;
  rank: 41;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x42 extends BaseP023F12 {
  kind: 'k-P023F12-42';
  label: string;
  weight: number;
  rank: 42;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x43 extends BaseP023F12 {
  kind: 'k-P023F12-43';
  label: string;
  weight: number;
  rank: 43;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x44 extends BaseP023F12 {
  kind: 'k-P023F12-44';
  label: string;
  weight: number;
  rank: 44;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x45 extends BaseP023F12 {
  kind: 'k-P023F12-45';
  label: string;
  weight: number;
  rank: 45;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x46 extends BaseP023F12 {
  kind: 'k-P023F12-46';
  label: string;
  weight: number;
  rank: 46;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export interface RecP023F12x47 extends BaseP023F12 {
  kind: 'k-P023F12-47';
  label: string;
  weight: number;
  rank: 47;
  tags: readonly string[];
  payload: { values: number[]; flags: Record<string, boolean>; note?: string };
}

export type AnyP023F12 =
  | RecP023F12x0
  | RecP023F12x1
  | RecP023F12x2
  | RecP023F12x3
  | RecP023F12x4
  | RecP023F12x5
  | RecP023F12x6
  | RecP023F12x7
  | RecP023F12x8
  | RecP023F12x9
  | RecP023F12x10
  | RecP023F12x11
  | RecP023F12x12
  | RecP023F12x13
  | RecP023F12x14
  | RecP023F12x15
  | RecP023F12x16
  | RecP023F12x17
  | RecP023F12x18
  | RecP023F12x19
  | RecP023F12x20
  | RecP023F12x21
  | RecP023F12x22
  | RecP023F12x23
  | RecP023F12x24
  | RecP023F12x25
  | RecP023F12x26
  | RecP023F12x27
  | RecP023F12x28
  | RecP023F12x29
  | RecP023F12x30
  | RecP023F12x31
  | RecP023F12x32
  | RecP023F12x33
  | RecP023F12x34
  | RecP023F12x35
  | RecP023F12x36
  | RecP023F12x37
  | RecP023F12x38
  | RecP023F12x39
  | RecP023F12x40
  | RecP023F12x41
  | RecP023F12x42
  | RecP023F12x43
  | RecP023F12x44
  | RecP023F12x45
  | RecP023F12x46
  | RecP023F12x47;

export type KindP023F12 = AnyP023F12['kind'];

export type ByKindP023F12 = { [R in AnyP023F12 as R['kind']]: R };

export type PatchP023F12<T> = { [K in keyof T]?: T[K] extends object ? PatchP023F12<T[K]> : T[K] };

export type SummaryP023F12 = {
  [K in KindP023F12]: { count: number; weight: number; ranks: number[] };
};

const kindsP023F12 = ['k-P023F12-0', 'k-P023F12-1', 'k-P023F12-2', 'k-P023F12-3', 'k-P023F12-4', 'k-P023F12-5', 'k-P023F12-6', 'k-P023F12-7', 'k-P023F12-8', 'k-P023F12-9', 'k-P023F12-10', 'k-P023F12-11', 'k-P023F12-12', 'k-P023F12-13', 'k-P023F12-14', 'k-P023F12-15', 'k-P023F12-16', 'k-P023F12-17', 'k-P023F12-18', 'k-P023F12-19', 'k-P023F12-20', 'k-P023F12-21', 'k-P023F12-22', 'k-P023F12-23', 'k-P023F12-24', 'k-P023F12-25', 'k-P023F12-26', 'k-P023F12-27', 'k-P023F12-28', 'k-P023F12-29', 'k-P023F12-30', 'k-P023F12-31', 'k-P023F12-32', 'k-P023F12-33', 'k-P023F12-34', 'k-P023F12-35', 'k-P023F12-36', 'k-P023F12-37', 'k-P023F12-38', 'k-P023F12-39', 'k-P023F12-40', 'k-P023F12-41', 'k-P023F12-42', 'k-P023F12-43', 'k-P023F12-44', 'k-P023F12-45', 'k-P023F12-46', 'k-P023F12-47'] as const;

let seqP023F12 = 0;

export function createP023F12<K extends KindP023F12>(kind: K, label: string, weight: number): ByKindP023F12[K] {
  seqP023F12 += 1;
  const rank = kindsP023F12.indexOf(kind);
  const rec = {
    id: `${kind}-${seqP023F12}`,
    createdAt: seqP023F12,
    updatedAt: seqP023F12,
    revision: 1,
    meta: { seq: seqP023F12, source: label },
    kind,
    label,
    weight,
    rank,
    tags: [label, kind],
    payload: { values: [weight, rank], flags: { fresh: true }, note: label },
  };
  return rec as unknown as ByKindP023F12[K];
}

export function batchP023F12(count: number): AnyP023F12[] {
  const out: AnyP023F12[] = [];
  for (let idx = 0; idx < count; idx++) {
    out.push(createP023F12(kindsP023F12[idx % kindsP023F12.length], `item-${idx}`, idx * 0.5));
  }
  return out;
}

export function pickP023F12<T extends object, K extends keyof T>(obj: T, keys: readonly K[]): Pick<T, K> {
  const out = {} as Pick<T, K>;
  for (const key of keys) {
    out[key] = obj[key];
  }
  return out;
}

export function groupByP023F12(records: readonly AnyP023F12[]): Partial<{ [K in KindP023F12]: ByKindP023F12[K][] }> {
  const out: Partial<{ [K in KindP023F12]: ByKindP023F12[K][] }> = {};
  for (const rec of records) {
    const bucket = (out[rec.kind] ??= []);
    (bucket as AnyP023F12[]).push(rec);
  }
  return out;
}

export function summarizeP023F12(records: readonly AnyP023F12[]): Partial<SummaryP023F12> {
  const grouped = groupByP023F12(records);
  const out: Partial<SummaryP023F12> = {};
  for (const kind of kindsP023F12) {
    const bucket = grouped[kind];
    if (!bucket) continue;
    out[kind] = {
      count: bucket.length,
      weight: bucket.reduce((sum, r) => sum + r.weight, 0),
      ranks: bucket.map((r) => r.rank as number),
    };
  }
  return out;
}

export function mergeP023F12<T extends BaseP023F12>(target: T, patch: PatchP023F12<T>): T {
  return { ...target, ...patch, revision: target.revision + 1 };
}

export function totalWeightP023F12(records: readonly AnyP023F12[]): number {
  const summary = summarizeP023F12(records);
  let total = 0;
  for (const kind of kindsP023F12) {
    total += summary[kind]?.weight ?? 0;
  }
  return total;
}
